<%= form_for(@new_message, remote: true) do |f| %>

  <%= f.label :auction %>
  <%= f.select (:auction_id, @user.auctions) %>

  <%= f.label :bid %>
  <%= f.select (:bid_id,)%>


  <%= f.label :content %>
  <%= f.text_field :content %>

  <%= f.submit "Send it!" %>
<% end %>
