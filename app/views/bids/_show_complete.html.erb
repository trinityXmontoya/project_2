

<%= render '/bids/bid' %>

<% if @bids != nil %>
<% @bids.each do |bid| %>
bid on
<%= link_to bid.auction do %>
  <%= bid.auction.title %>
  <%= bid.auction.calculate_time_left %>
  <img src="<%= bid.auction.img_url %>">
<% end %>
at
<%= bid.created_at.strftime("%D%H")%>
<% end %>
<% end %>
